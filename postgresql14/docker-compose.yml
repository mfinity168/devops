version: '3'
services:
  postgres14:
    image: postgres:14.0
    container_name: postgres14
    restart: always
    environment:
      POSTGRES_USER: USER
      POSTGRES_PASSWORD: PASSWORD
      TZ: "Asia/Bangkok"
    command: postgres -c 'config_file=/etc/postgresql/postgresql.conf'
    volumes:
      - ./postgres14/pgdata:/var/lib/postgresql/data
    ports:
      - 5432:5432
    # network_mode: host
    # networks:
    #   private-network:
    #     ipv4_address: 1.1.1.1
    # networks:
    #   private-network:
    #     external:
    #       name: private-network
